import { TestBed } from '@angular/core/testing';

import { ApplicantworkexperienceService } from './applicantworkexperience.service';

describe('ApplicantworkexperienceService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: ApplicantworkexperienceService = TestBed.get(ApplicantworkexperienceService);
    expect(service).toBeTruthy();
  });
});

export class DocumentType {
    private id: string;
    private docTypeCode: string;
    private docTypeName: string;
    private flag: boolean;

    constructor(docTypeCode: string,
                docTypeName: string,
                flag: boolean){
                    
                }
}
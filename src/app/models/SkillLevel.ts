export class SkillLevel {
    private id: string;
    private skillLevelCode: string;
    private skillLevelName: string;
    private activeState: boolean;

    constructor(skillLevelCode: string,
                skillLevelName: string,
                activeState: boolean){
                    
                }
}
import { User } from './User';

export class ApplicantSalary {
    private id: string;
    private user: User;
    private salaryExpected: number;

    constructor(user: User,
                salaryExcepted: number){
                    
                }
}